using Lab27WeatherApi.Models;
using System;
using System.Collections.Generic;
using System.Linq;
using System.Net;
using System.Net.Http;
using System.Threading.Tasks;
using System.Web.Http;

namespace Lab27WeatherApi.Controllers
{
    public class ValuesController : ApiController
    {
        // GET api/values
        public IEnumerable<string> Get()
        {
            return new string[] { "value1", "value2" };
        }

        // GET api/values/5
        public string Get(int longitude, int latitude)
        {
            //string url = "";
            //using (HttpResponseMessage response = await ApiHelper.ApiClient.FetAsunce(Url))
            //    WeatherDataModel data = await ResponseMessage.Content.ReadAsAsync<WeatherDataModel>();
            string data = "value";
            return data; 
                }

        // POST api/values
        public void Post([FromBody]string value)
        {
        }

        // PUT api/values/5
        public void Put(int id, [FromBody]string value)
        {
        }

        // DELETE api/values/5
        public void Delete(int id)
        {
        }
    }
}
